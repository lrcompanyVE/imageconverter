# LigtherConverter

Un convertidor de imágenes ligero y multiplataforma construido con Python y CustomTkinter. Funciona de forma nativa en **Linux, Windows y macOS**.

## 🚀 Requisitos previos (Linux)
Si estás en una distribución basada en Ubuntu/Debian, necesitas instalar Tkinter en el sistema antes de empezar:
```bash
sudo apt update
sudo apt install python3-tk
```

## 🛠️ Instalación y Uso

1. Clona este repositorio o descarga el código.
2. Instala las dependencias necesarias:
   ```bash
   pip install -r requirements.txt
   ```
3. Ejecuta la aplicación:
   ```bash
   python3 converterv2.py
   ```

## 📋 Características
- Conversión entre múltiples formatos: PNG, JPG, WEBP e ICON.
- Detección inteligente de rutas multiplataforma.
